import { N3NgFlyDirective } from './fly.directive';

describe('N3NgFlyDirective', () => {
  it('should create an instance', () => {
    const directive = new N3NgFlyDirective();
    expect(directive).toBeTruthy();
  });
});
