import { ColumnRefDirective } from './column-ref.directive';

describe('ColumnRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnRefDirective();
    expect(directive).toBeTruthy();
  });
});
