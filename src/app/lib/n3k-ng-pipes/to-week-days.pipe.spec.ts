import { ToWeekDaysPipe } from './to-week-days.pipe';

describe('ToWeekDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new ToWeekDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
